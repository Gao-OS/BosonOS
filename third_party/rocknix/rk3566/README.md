# RK3566 Reference Imports

This directory is reserved for ROCKNIX-derived RK3566 resources such as kernel
config, DTS files, patches, bootloader references, and board mapping notes.

The first milestone does not import hardware files. Add imported resources only
with preserved license metadata and an update to `../sources.lock`.
